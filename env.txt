NEXT_PUBLIC_SUPABASE_URL={{SUPABASE_URL}}
NEXT_PUBLIC_SUPABASE_ANON_KEY={{SUPABASE_ANON_KEY}}
SUPABASE_SERVICE_ROLE_KEY={{SUPABASE_SERVICE_ROLE_KEY}}
LLM_API_KEY_OR_MOCK={{LLM_API_KEY_OR_MOCK}}
MODEL_NAME={{MODEL_NAME}}
SIGNING_KEY={{SIGNING_KEY}}

# Hybrid Generation Configuration (Optional)
DEFAULT_GENERATION_STRATEGY=single  # 'single' or 'multi'
ENABLE_HYBRID_ROUTING=true          # Set to 'false' to disable intelligent routing
COMPLEXITY_THRESHOLD=70             # 0-100, higher = more likely to use multi-agent

